#include <stdio.h>
#include <conio.h>

int main(void){
    int i=0, j=0, n=0, d=0, parar=0;
    printf("Digite o valor de N: ");
    scanf("%d", &n);
    printf("Digite o valor de I: ");
    scanf("%d", &i);
    printf("Digite o valor de J: ");
    scanf("%d", &j);
    while (parar<=n){
          if(i%d==0 && j%d==0){
                 printf("Divisores de I: %d", i);
                 printf("Divisores de J: %d", j);
                 }
                 d++;
                 parar++;
                 }
     getch();
     return 0;
}                 
